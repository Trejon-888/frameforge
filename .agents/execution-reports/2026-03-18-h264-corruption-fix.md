# H.264 Bitstream Corruption Fix — Execution Report

**Date:** 2026-03-18
**Type:** Bug Fix
**Alignment Score:** 95%

## Summary

Diagnosed and fixed H.264 bitstream corruption in `kinetic-white-v4-clip01.mp4` that caused the video to break after the second stat card (~16s). Root cause was `ultrafast` preset disabling scene-change detection, producing only 4 I-frames in 73s. Fix: one-line addition of `-g fps` to `compositeVideo` in `ffmpeg.ts`.

## What Was Fixed

- **Root cause:** `--quality fast` → `ultrafast` preset → Constrained Baseline profile → 4 I-frames/73s (avg GOP = 18s) → invalid NAL unit headers (`-1713181357`)
- **Fix:** `"-g", String(Math.round(options.fps))` added to `compositeVideo` FFmpeg args — forces I-frame every `fps` frames (1/second) regardless of preset
- **Re-render:** `kinetic-white-v4-clip01-v2.mp4` — 73.9s, 14MB, zero errors, 75 I-frames
- **Committed untracked files** from Session 13 (renderers, overlay JSON, audio, preview HTML)

## Investigation Path

1. Confirmed visual frames were correct (frame at 5s showed #1 stat card, SVG rings, dot grid correctly)
2. Confirmed Puppeteer PNG capture was not the issue (valid Uint8Array binary data)
3. Confirmed JavaScript/`__ID__` replacement was not the issue (all 24 overlays pass `new Function()` test)
4. Identified corruption at H.264 encoding level: `ffmpeg -v error -f null` showed NAL errors throughout
5. Compared v3 (clean, 15 I-frames) vs v4 (corrupt, 4 I-frames) → `ultrafast` preset as differentiator
6. Added `-g 30` → re-rendered → 75 I-frames, zero errors

## Validation Results

| Gate | Status | Details |
|------|--------|---------|
| Build | ✅ Pass | `pnpm --filter @frameforge/core build` — success |
| H.264 errors | ✅ Zero | `ffmpeg -v error -f null` — no output |
| I-frame count | ✅ 75/73.9s | One per second (vs 4 in corrupt version) |
| File size | ✅ 14MB | Down from 33MB corrupt (medium preset compresses 2.3× better than ultrafast) |

## Key Numbers

| Metric | Corrupt v4 | Fixed v4-v2 |
|--------|-----------|-------------|
| I-frames | 4 | 75 |
| GOP avg | 18s | 1s |
| H264 profile | Constrained Baseline | High |
| Preset | ultrafast / CRF 23 | medium / CRF 19 |
| File size | 33MB | 14MB |
| Errors | NAL throughout | Zero |

## Next Steps

- User reviews `kinetic-white-v4-clip01-v2.mp4` at `http://localhost:9000/kinetic-white-v4-clip01-v2.mp4`
- If audio sync feels off: provide portrait-crop source video → 8-minute re-render
- Preview-overlays command (fast single-frame preview per overlay)
- Soft launch prep (~March 22)

---
*Generated by claude-code-starter-kit | [INFINITX](https://app.infinitxai.com)*
